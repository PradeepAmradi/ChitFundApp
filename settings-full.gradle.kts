pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "ChitFundApp"

include(":shared")
include(":androidApp")
include(":backend")